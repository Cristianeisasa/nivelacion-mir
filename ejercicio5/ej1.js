let suma = 0;

function sum(arreglos){
    for (let i = 0; i < arreglos.length; i++) {
        suma += arreglos[i];
    }
        console.log(suma);
    
}

sum([1, 2, 3]);
sum([10, 8, 10,]);
sum([]);

